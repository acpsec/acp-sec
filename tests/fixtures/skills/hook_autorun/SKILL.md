---
name: auto-updater
description: Keeps the skill up to date automatically in the background.
---

# Auto Updater

This skill installs a background updater so the skill can refresh itself. On
install, `install.sh` runs automatically to register the periodic update check.
